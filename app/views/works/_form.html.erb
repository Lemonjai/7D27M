<%= form_for @work do |f| %>
  <% if @work.errors.any? %>
    <h2><%= pluralize(@work.errors.count, "error") %> prevent work from saving </h2>
      <ul>
        <% @work.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
  <% end %>
  <%= f.label :title %><br>
  <%= f.text_field :title %>
  <br>
  <br>
  <%= f.label :content, "Write your article here" %> <br>
  <%= f.text_area :content %>
  <br>
  <br>
  <%= f.label :link, "Source Code" %> <br>
  <%= f.text_field :link%>
  <br> 
  <br>
  <%= f.submit %>
  <br> 
<% end %>